(* TEST
  include refute;
*)

let test (f : (int -> int) -> int) : unit =
  let a = fun x -> x + 1 in
  let b = fun x -> 1 + x in
  assert (f a == f b) (* ERROR: x + 1 is intensionally different from 1 + x *)