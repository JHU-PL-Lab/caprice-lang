(* TEST
  include refute;
*)

let _ : ((int -> int) -> int) -> unit =
  fun f ->
    let a = fun x -> x + 1 in
    let b = fun x -> 1 + x in
    assert (f a == f b) (* ERROR: x + 1 is intensionally different from 1 + x *)