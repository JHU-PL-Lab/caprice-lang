(* TEST
  include exhaust;
*)

let t = int * (int -> int)

let f (g : t -> int) : unit =
  let i = g (0, fun a -> a) in
  let _ = g (1, fun b -> b*2) in
  let j = g (0, fun c -> c*0) in
  let k = g (0, fun d -> d*3) in
  ()
