(* TEST
  include refute;
*)

(*
  Using nondeterministic function where a deterministic one is expected.
*)
let test (f : (int -> int) -> int) (g : int ~> int) : int =
  f g (* ERROR: g is not deterministic *)